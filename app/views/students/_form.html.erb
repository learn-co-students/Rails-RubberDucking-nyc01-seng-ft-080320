<h3>Form for Student</h3>

<% if flash[:my_errors] %>
    <h2><%= pluralize(flash[:my_errors].count, "error") %> prohibited this article from being saved:</h2>
 
    <ul>
    <% flash[:my_errors].each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
<% end %>

<%= form_for @student do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :mod %>
    <%= f.text_field :mod %>

    <%= f.submit %>
<% end %>