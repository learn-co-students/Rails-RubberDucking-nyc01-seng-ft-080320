<h1><%= @student.name %></h1>
<h3>My Ducks!</h3>
<ul> 
<% @student.ducks.each do |duck| %>
<li><%= link_to duck.name, duck_path(duck) %></li>
<% end%>
</ul>

<%= button_to "delete", student_path(@student), method: :delete %>
<%= button_to "index", students_path , method: :get %>
